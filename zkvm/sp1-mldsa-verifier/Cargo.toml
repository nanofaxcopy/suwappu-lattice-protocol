[package]
name = "sp1-mldsa-verifier"
version = "0.1.0"
edition = "2021"
description = "SP1 zkVM circuit: proves ML-DSA-65 signature validity for ETP bridge"

[dependencies]
sp1-zkvm = "4.0"
sha3 = "0.10"
# Pure-Rust ML-DSA-65 (FIPS 204) — compatible with SP1's RISC-V target
ml-dsa = "0.1.0-rc.8"
